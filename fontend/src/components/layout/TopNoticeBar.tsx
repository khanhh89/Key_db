import type { Language, SystemConfig } from '../../types';

interface TopNoticeBarProps {
  lang: Language;
  config: SystemConfig;
}

export function TopNoticeBar(_props: TopNoticeBarProps) {
  return null;
}

